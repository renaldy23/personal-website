import Typed from 'typed.js'

const typed = new Typed('#heading', {
  strings: ['Fullstack Developer', 'Mobile Developer', 'Software Implementor'],
  typeSpeed: 50,
  shuffle: true,
  loop: true,
  cursorChar: '_',
  backSpeed: 50,
})

export default typed
